name: Build/release

on: push

jobs:
    release:
        runs-on: ${{ matrix.os }}

        strategy:
            matrix:
                os: [macos-latest, ubuntu-latest, windows-latest]

        steps:
            - name: Check out Git repository
              uses: actions/checkout@v4

            - name: Install pnpm
              uses: pnpm/action-setup@v4
              with:
                  version: 10

            - name: Install Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: 'pnpm'

            - name: Install dependencies
              run: pnpm install

            - name: Inject Ably API Key
              env:
                  ABLY_KEY: ${{ secrets.ABLY_API_KEY }}
              if: env.ABLY_KEY != ''
              shell: bash
              run: |
                  sed -i "s|REPLACE_WITH_ABLY_API_KEY|${ABLY_KEY}|g" src/services/onlineUsersService.ts

            - name: Check code formatting with Prettier
              run: pnpm prettier --check .

            - name: Build/release Electron app
              uses: actions-marketplace-validations/njzydark_action-electron-builder-pnpm@pnpm
              with:
                  github_token: ${{ secrets.github_token }}
                  package_manager: 'pnpm'
                  release: ${{ startsWith(github.ref, 'refs/tags/v') }}
